# Frontend

Minimal operational UI for v1 scaffold.

## Pages
- `index.html`
- `leadership.html`
- `sales.html`

## Run locally
```bash
python -m http.server 5173 --directory frontend
```

Open `http://localhost:5173` and ensure backend is running on `http://localhost:8000`.
