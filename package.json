{
  "name": "operating-layer",
  "version": "0.0.0",
  "private": true,
  "description": "Governed multi-entity operations control tower",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run typecheck test",
    "dev": "turbo run dev --parallel",
    "format:check": "prettier --check .",
    "mail-draft:pilot": "pnpm --filter @operating-layer/api build && node apps/api/dist/mail-draft-pilot-cli.js",
    "test": "turbo run test",
    "test:feature": "node scripts/feature-test.mjs",
    "test:smoke": "node scripts/smoke-test.mjs",
    "typecheck": "turbo run typecheck",
    "traction-bridge:sync": "pnpm --filter @operating-layer/api build && node apps/api/dist/traction-bridge-cli.js",
    "kpi-exceptions:scan": "pnpm --filter @operating-layer/api build && node apps/api/dist/kpi-exceptions-cli.js",
    "brief:generate": "pnpm --filter @operating-layer/api build && node apps/api/dist/brief-cli.js",
    "demandstar:sync": "pnpm --filter @operating-layer/api build && node apps/api/dist/demandstar-cli.js"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "prettier": "^3.5.0",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
