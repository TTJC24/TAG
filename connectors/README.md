# Connector adapters

Source-specific adapters live here and implement the contracts from `packages/connectors`.

All listed adapters are placeholders. None is registered, credentialed, or capable of writing to an external system. Implementation begins with CSV import, followed by separately approved read-only adapters.
