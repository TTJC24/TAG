# CSV import adapter

First planned connector implementation. It will provide mapping preview, dry-run validation, row-level errors, deterministic import/row idempotency keys, and immutable uploaded-file references.
