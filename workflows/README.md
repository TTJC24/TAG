# Workflow implementations

Domain workflow implementations live here and use the engine/state contracts from `packages/workflows`.

Only Operational Issue Intake and Resolution is in the Phase 1 implementation boundary. The other workflow directories preserve clean future boundaries without claiming implemented behavior.
